# Backend

Express-based layered backend.

Structure:

- `controllers/` - request handlers
- `services/` - business logic
- `repositories/` - data access
- `models/` - domain models
- `routes/` - express routes

Example endpoint: `GET /api/hello` returns JSON.

Run:

```powershell
cd backend
npm install
npm start
```

Then visit: http://localhost:3000/api/hello
# IT342_G4_Morre_Lab1
Mini App – User Registration &amp; Authentication Activity
